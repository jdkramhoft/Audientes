package a3.audientes;

import android.app.Application;
import android.content.Context;

public class Audientes extends Application {
    @Override
    protected void attachBaseContext(Context base) {
        super.attachBaseContext(base);
    }
}